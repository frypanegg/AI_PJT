# Home Assistant 접속
HA_URL=http://homeassistant.local:8123
HA_TOKEN=<장기 액세스 토큰>

# 레지스트리 (비우면 config/devices.yaml)
REGISTRY_PATH=

# 이력 DB. 도커 밖(맥에서 직접 실행)에서는 로컬 파일 경로를 쓴다.
DB_PATH=home_core.db

# HA 폴링 주기(초). 0 이면 폴링하지 않는다.
POLL_INTERVAL=10
